class Position {
  public bufferIndex = 0;
  public mask = 0;
}

function inflate_block(buf: Uint8array, pos: Position,
                       litdict: any, distdict: any, buf: IndexedBuffer<Uint8array>): void {
}

export function inflate(buf: Uint8Array, output: Uint8Array): void {
}
